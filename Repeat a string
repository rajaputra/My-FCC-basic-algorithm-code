function repeatStringNumTimes(str, num) {
  // repeat after me
  var repeatStr ="";
  var i =1;
  while (i <= num);{
    repeatStr += str;
    i++;
  }
  // for (var i=0; i < num; i++ ){
 //   repeatStr
  return repeatStr;
}

repeatStringNumTimes("abc", 3);
